import React, { useEffect, useState } from "react";
import { useNavigate } from "react-router-dom";
import { Banknote, Check, CreditCard, Wallet } from "lucide-react";
import { formatPrice } from "@/lib/cafeData";
import { useCart } from "@/lib/CartContext";
import Receipt from "@/components/Receipt";
import { apiPost } from "@/lib/apiClient";

export default function Payment() {
  const navigate = useNavigate();
  const { items, totals, orderType, table, billDiscount, clear } = useCart();
  const [method, setMethod] = useState("cash"), [split, setSplit] = useState(null), [confirming, setConfirming] = useState(false), [receipt, setReceipt] = useState(null);
  const handleConfirm = async () => {
    setConfirming(true);
    try {
      const saved = await apiPost("/orders", { order_type: orderType, table_or_address: orderType === "dine-in" || orderType === "delivery" ? table || null : null, payment_method: split ? "split" : method, lines: items.map((line) => ({ menu_item_id: line.itemId, qty: line.qty, size: line.size?.name, temperature: line.temperature, sugar: line.sugar, ice: line.ice, note: line.note, addon_ids: [] })) });
      setReceipt({ order_number: saved.order_number, order_type: saved.order_type, items: items.map((line) => ({ name: line.name, qty: line.qty, lineTotal: line.lineTotal })), subtotal: Number(saved.subtotal), discount_amount: 0, delivery_fee: Number(saved.delivery_fee), tax: Number(saved.tax), total: Number(saved.total) });
    } catch (err) { alert("Couldn't save this order: " + err.message); } finally { setConfirming(false); }
  };
  useEffect(() => { if (!receipt) return undefined; const printed = () => { clear(); navigate("/order"); }; window.addEventListener("afterprint", printed); return () => window.removeEventListener("afterprint", printed); }, [receipt, clear, navigate]);
  const place = orderType === "dine-in" ? `Table ${table || "—"}` : orderType === "delivery" ? `Delivery — ${table || "no address"}` : "Takeaway";
  return <div className="min-h-screen px-8 py-7 max-w-3xl mx-auto"><div className="mb-6"><div className="text-sm font-medium">Counter 1 · Cashier</div><div className="text-xs text-[hsl(var(--muted-foreground))]">Checkout</div></div><h1 className="text-[28px] font-medium mb-5">Payment</h1><section className="rounded-xl bg-[hsl(var(--card))] border p-5"><div className="flex justify-between mb-4"><span className="font-medium text-sm">{place}</span><span className="text-xs">{items.length} items</span></div>{items.map((line) => <div className="flex justify-between text-sm mb-2" key={line.lineId}><span>{line.qty}× {line.name}{line.note && <small className="block italic">“{line.note}”</small>}</span><span>{formatPrice(line.lineTotal)}</span></div>)}<div className="border-t mt-4 pt-3 space-y-1"><Row label="Subtotal" value={formatPrice(totals.subtotal)} />{totals.deliveryFee > 0 && <Row label="Delivery fee" value={formatPrice(totals.deliveryFee)} />}{billDiscount && <Row label={`Discount · ${billDiscount.code}`} value={`−${formatPrice(totals.discountAmount)}`} />}<Row label="Tax" value={formatPrice(totals.tax)} /><div className="flex justify-between pt-2 font-medium"><span>Total</span><span className="text-2xl text-[hsl(var(--primary))]">{formatPrice(totals.total)}</span></div></div></section><section className="mt-5"><div className="text-sm mb-2">Payment method</div><div className="grid grid-cols-3 gap-3">{[{ id: "cash", label: "Cash", Icon: Banknote }, { id: "card", label: "Card", Icon: CreditCard }, { id: "e-wallet", label: "E-wallet", Icon: Wallet }].map(({ id, label, Icon }) => <button key={id} onClick={() => { setMethod(id); setSplit(null); }} className={`h-16 rounded-xl border flex flex-col items-center justify-center ${method === id && !split ? "border-[hsl(var(--primary))]" : ""}`}><Icon size={20} /><span className="text-xs">{label}</span></button>)}</div><button className="mt-2 text-xs underline" onClick={() => setSplit(split ? null : { method1: "cash", amount1: Math.round(totals.total / 2) })}>{split ? "Cancel split" : "Split payment across two methods"}</button>{split && <div className="mt-3 border rounded-xl p-4"><select value={split.method1} onChange={(e) => setSplit({ ...split, method1: e.target.value })} className="h-10 border rounded px-2"><option value="cash">Cash</option><option value="card">Card</option><option value="e-wallet">E-wallet</option></select><input aria-label="Split amount" type="number" value={split.amount1} onChange={(e) => setSplit({ ...split, amount1: Number(e.target.value) })} className="h-10 border rounded px-2 ml-2" /><div className="text-xs mt-2">Balance on second method: {formatPrice(Math.max(0, totals.total - split.amount1))}</div></div>}</section><section className="mt-6"><button onClick={handleConfirm} disabled={confirming || !items.length || receipt} className="w-full h-12 rounded bg-[hsl(var(--primary))] text-white disabled:opacity-40"><Check className="inline mr-2" size={16} />{confirming ? "Processing..." : `Confirm payment · ${formatPrice(totals.total)}`}</button><div className="grid grid-cols-2 gap-3 mt-2"><button onClick={() => navigate("/order")} className="h-11 border rounded">Save for later</button><button onClick={() => navigate("/order")} className="h-11 border rounded">Cancel</button></div>{receipt && <button onClick={() => window.print()} className="w-full h-12 mt-3 rounded bg-[hsl(var(--primary))] text-white">Print receipt</button>}</section><Receipt order={receipt} /></div>;
}
function Row({ label, value }) { return <div className="flex justify-between text-sm"><span>{label}</span><span>{value}</span></div>; }
